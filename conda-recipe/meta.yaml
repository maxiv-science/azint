{% set version = "0.8.2" %}

package:
  name: azint
  version: {{ version }}

source:
  path: ..

build:
  number: 2
  script: pip install .
requirements:
  build:
    - {{ compiler('cxx') }}

  host:
    - python
    - numpy
    - pybind11
    
  run:
    - python
    - numpy
    
test:
  source_files:
    - tests/*.py
    - tests/test.poni
  requires:
    - pytest
  commands:
    - pytest tests/

 
